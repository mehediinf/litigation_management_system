import 'package:flutter/material.dart';

class ComplianceCaseStatusUpdatePage extends StatelessWidget {
  const ComplianceCaseStatusUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Case Status Update')),
      body: const Center(child: Text('Compliance Case Status Update')),
    );
  }
}
