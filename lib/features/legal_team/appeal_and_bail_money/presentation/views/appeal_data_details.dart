import 'package:flutter/material.dart';

class AppealDataDetailsPage extends StatelessWidget {
  const AppealDataDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Data Details')),
      body: const Center(child: Text('Data Details')),
    );
  }
}
