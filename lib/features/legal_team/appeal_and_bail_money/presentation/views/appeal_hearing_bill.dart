import 'package:flutter/material.dart';

class AppealHearingBillPage extends StatelessWidget {
  const AppealHearingBillPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hearing Bill')),
      body: const Center(child: Text('Hearing Bill')),
    );
  }
}
