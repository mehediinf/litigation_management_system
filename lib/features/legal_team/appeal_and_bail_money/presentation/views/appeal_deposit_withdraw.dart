import 'package:flutter/material.dart';

class AppealDepositWithdrawPage extends StatelessWidget {
  const AppealDepositWithdrawPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Deposit & Withdraw')),
      body: const Center(child: Text('Deposit & Withdraw')),
    );
  }
}
