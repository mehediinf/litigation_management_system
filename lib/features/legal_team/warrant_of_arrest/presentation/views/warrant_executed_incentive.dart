import 'package:flutter/material.dart';

class ExecutedIncentivePage extends StatelessWidget {
  const ExecutedIncentivePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Executed Incentive')),
      body: const Center(child: Text('Executed Incentive')),
    );
  }
}
