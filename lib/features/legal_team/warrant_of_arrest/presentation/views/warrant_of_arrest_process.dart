import 'package:flutter/material.dart';

class WarrantOfArrestProcessPage extends StatelessWidget {
  const WarrantOfArrestProcessPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Arrest Process')),
      body: const Center(child: Text('Warrant Of Arrest Process')),
    );
  }
}
